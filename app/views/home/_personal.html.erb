<%= turbo_frame_tag "gallery" do %>
  <div id="personal" class="d-block">
    <div class="d-flex flex-column align-items-center gap-5">
      <% works.each do |work| %>
        <div class="text-center" style="max-width: 600px;">
          <% if work.image.attached? %>
            <%= image_tag work.image, class: "img-fluid mb-3",
                          style: "aspect-ratio: 1/1; object-fit: cover; width: 100%;" %>
          <% else %>
            <%= image_tag work.image_url, class: "img-fluid mb-3",
                          style: "aspect-ratio: 1/1; object-fit: cover; width: 100%;" %>
          <% end %>
          <p class="mb-1 fw-medium" style="color: #f1f1f1;"><%= work.title %></p>
          <p class="text-secondary small"><%= work.email %></p>
        </div>
      <% end %>
    </div>
  </div>
<% end %>